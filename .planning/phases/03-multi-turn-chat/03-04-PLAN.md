---
phase: 03-multi-turn-chat
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - frontend/src/App.tsx
autonomous: true
requirements:
  - CHAT-02

must_haves:
  truths:
    - "Sidebar shows real conversation sessions (title = first user message truncated, subtitle = turn count + relative time)"
    - "Compact and Clear buttons are always visible in sidebar, disabled when no messages exist"
    - "New conversation button generates a new conversation_id and clears the chat state"
    - "Clicking Compact in sidebar calls chatRef.current.compact() which triggers the ChatBox handleCompact flow"
    - "Clicking Clear in sidebar shows inline confirmation then calls chatRef.current.clear()"
    - "Active conversation is highlighted in sidebar with aws.squidLight background"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Real session list from state, Compact/Clear sidebar controls wired to ChatBox ref, New conversation wiring"
      contains: "chatRef"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/Chat/ChatBox.tsx"
      via: "useRef<ChatBoxHandle> + forwardRef ChatBox"
      pattern: "chatRef\\.current\\.compact\|chatRef\\.current\\.clear"
    - from: "frontend/src/App.tsx"
      to: "sessions state"
      via: "onSessionUpdate callback from ChatBox"
      pattern: "onSessionUpdate"
---

<objective>
Update App.tsx to replace mock conversation data with real session state driven by ChatBox callbacks, add sidebar Compact/Clear controls wired to ChatBox's useImperativeHandle ref, and wire the New conversation button to create fresh sessions.

Purpose: Completes the Phase 3 UI — the sidebar becomes live, showing real conversations with turn counts and relative timestamps, with functional Compact/Clear controls.
Output: Updated App.tsx with real session list, ChatBox ref wiring, Compact/Clear sidebar controls.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-turn-chat/03-CONTEXT.md
@.planning/phases/03-multi-turn-chat/03-UI-SPEC.md
@frontend/src/App.tsx

<interfaces>
<!-- From Plan 03 (03-03-PLAN.md) — ChatBox now exports these -->
From frontend/src/components/Chat/ChatBox.tsx:
```typescript
export interface ChatBoxHandle {
  compact: () => Promise<void>
  clear: () => Promise<void>
  hasMessages: () => boolean
}

interface ChatBoxProps {
  conversationId?: string
  onSessionUpdate?: (id: string, messages: Message[], updatedAt: number) => void
}

export const ChatBox: React.ForwardRefExoticComponent<
  ChatBoxProps & React.RefAttributes<ChatBoxHandle>
>
```

<!-- Session shape needed in App.tsx -->
```typescript
interface Session {
  id: string
  title: string      // first user message, max 32 chars (truncated by noOfLines)
  turnCount: number  // number of user turns (messages where role === 'user')
  updatedAt: number  // timestamp for relative time display
}
```

<!-- Relative time helper (already defined in ChatBox.tsx — duplicate here for App.tsx) -->
```typescript
function getRelativeTime(ts: number): string {
  const diffMs = Date.now() - ts
  const diffMin = Math.floor(diffMs / 60000)
  if (diffMin < 1) return 'just now'
  if (diffMin < 60) return `${diffMin}m ago`
  const diffHr = Math.floor(diffMin / 60)
  if (diffHr < 24) return `${diffHr}h ago`
  const diffDay = Math.floor(diffHr / 24)
  return diffDay === 1 ? 'Yesterday' : `${diffDay}d ago`
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace mock sessions with real state + wire ChatBox ref</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (full current file — understand sidebar layout to preserve, only update data layer)
    - frontend/src/components/Chat/ChatBox.tsx (updated in Plan 03 — ChatBoxHandle export, ChatBoxProps, onSessionUpdate)
    - .planning/phases/03-multi-turn-chat/03-UI-SPEC.md (§7 Conversation Session List spec + Copywriting §7)
  </read_first>
  <action>
**Complete rewrite of `frontend/src/App.tsx`**. Preserve all visual structure (sidebar layout, logo, footer nav, main content area). Replace data layer (MOCK_CONVERSATIONS removed, real sessions state added, ChatBox ref wired).

**Updated imports:**
```typescript
import { useState, useRef, useCallback } from 'react'
import {
  Box,
  Flex,
  VStack,
  HStack,
  Text,
  Button,
  Icon,
  useToast,
} from '@chakra-ui/react'
import { ChatBox } from './components/Chat/ChatBox'
import type { ChatBoxHandle } from './components/Chat/ChatBox'
import { KnowledgeBase } from './pages/KnowledgeBase'
import { MdAdd, MdBolt, MdOutlineArticle, MdCompress, MdClear } from 'react-icons/md'
```

**Types + helper (replace MockConversation + MOCK_CONVERSATIONS):**
```typescript
type Page = 'chat' | 'knowledge'

interface Session {
  id: string
  title: string      // derived from first user message (max 32 chars)
  turnCount: number  // count of user messages
  updatedAt: number  // Date.now() at last update
}

function getRelativeTime(ts: number): string {
  const diffMs = Date.now() - ts
  const diffMin = Math.floor(diffMs / 60000)
  if (diffMin < 1) return 'just now'
  if (diffMin < 60) return `${diffMin}m ago`
  const diffHr = Math.floor(diffMin / 60)
  if (diffHr < 24) return `${diffHr}h ago`
  const diffDay = Math.floor(diffHr / 24)
  return diffDay === 1 ? 'Yesterday' : `${diffDay}d ago`
}
```

**App component (full replacement):**
```typescript
function App() {
  const [page, setPage] = useState<Page>('chat')
  const [sessions, setSessions] = useState<Session[]>([])
  const [activeConvId, setActiveConvId] = useState<string | undefined>(undefined)
  const [clearConfirming, setClearConfirming] = useState(false)

  // Ref to ChatBox for Compact/Clear sidebar controls (useImperativeHandle bridge)
  const chatRef = useRef<ChatBoxHandle>(null)
  const toast = useToast()

  // Called by ChatBox when messages change (new turn, clear, compact)
  const handleSessionUpdate = useCallback(
    (id: string, messages: Array<{ role: string; content: string }>, updatedAt: number) => {
      setSessions(prev => {
        const existing = prev.find(s => s.id === id)
        const firstUserMsg = messages.find(m => m.role === 'user')?.content ?? ''
        const title = firstUserMsg.slice(0, 32) || 'New conversation'
        const turnCount = messages.filter(m => m.role === 'user').length

        if (existing) {
          return prev.map(s =>
            s.id === id ? { ...s, title, turnCount, updatedAt } : s
          )
        }
        // New session — add to top
        return [{ id, title, turnCount, updatedAt }, ...prev]
      })
      setActiveConvId(id)
    },
    []
  )

  // Sidebar Compact handler — delegates to ChatBox via ref
  const handleSidebarCompact = useCallback(async () => {
    if (!chatRef.current?.hasMessages()) return
    await chatRef.current.compact()
  }, [])

  // Sidebar Clear handler — shows inline confirmation, then delegates
  const handleSidebarClearConfirm = useCallback(async () => {
    await chatRef.current?.clear()
    setClearConfirming(false)
  }, [])

  // New conversation — generate new UUID, clear active session
  const handleNewConversation = useCallback(() => {
    const newId = crypto.randomUUID()
    setActiveConvId(newId)
    setPage('chat')
    setClearConfirming(false)
  }, [])

  const hasMessages = chatRef.current?.hasMessages() ?? false

  return (
    <Flex h="100vh" overflow="hidden" bg="gray.100">
      {/* Sidebar */}
      <Flex as="aside" direction="column" w="260px" flexShrink={0} bg="aws.squid" color="gray.100">
        {/* Logo — unchanged */}
        <HStack px={4} py={4} borderBottom="1px solid" borderColor="aws.squidLight" spacing={3}>
          <Flex w={8} h={8} borderRadius="md" bg="aws.orange" align="center" justify="center" flexShrink={0}>
            <Icon as={MdBolt} color="aws.squid" boxSize={5} />
          </Flex>
          <Box>
            <Text fontSize="xs" fontWeight="bold" color="white" lineHeight="none">AWS</Text>
            <Text fontSize="10px" color="gray.400" lineHeight="none" mt="2px">Architecture Advisor</Text>
          </Box>
        </HStack>

        {/* New conversation button */}
        <Box px={3} pt={3}>
          <Button
            leftIcon={<Icon as={MdAdd} />}
            variant="outline"
            size="sm"
            w="full"
            justifyContent="flex-start"
            color="gray.300"
            borderColor="aws.squidLight"
            _hover={{ bg: 'aws.squidLight', color: 'white' }}
            onClick={handleNewConversation}
          >
            New conversation
          </Button>
        </Box>

        {/* Conversation session list — replaces MOCK_CONVERSATIONS (D-07, UI-SPEC §7) */}
        <VStack flex={1} overflowY="auto" px={2} py={3} spacing={0} align="stretch">
          <Text
            px={2} pb={1}
            fontSize="10px" fontWeight="semibold" textTransform="uppercase"
            letterSpacing="wider" color="gray.500"
          >
            Recent
          </Text>

          {sessions.length === 0 ? (
            <Text px={2} pt={2} fontSize="11px" color="gray.600" fontStyle="italic">
              No conversations yet. Send your first message.
            </Text>
          ) : (
            sessions.map(s => (
              <Box
                key={s.id}
                as="button"
                textAlign="left"
                px={3} py={2}
                borderRadius="md"
                bg={activeConvId === s.id ? 'aws.squidLight' : 'transparent'}
                _hover={{ bg: activeConvId === s.id ? 'aws.squidLight' : 'whiteAlpha.100' }}
                onClick={() => { setActiveConvId(s.id); setPage('chat') }}
                transition="background 0.15s"
              >
                <Text fontSize="sm" color="gray.200" fontWeight="medium" noOfLines={1}>
                  {s.title}
                </Text>
                <Text fontSize="11px" color="gray.500" noOfLines={1}>
                  {s.turnCount} turn{s.turnCount !== 1 ? 's' : ''} · {getRelativeTime(s.updatedAt)}
                </Text>
              </Box>
            ))
          )}
        </VStack>

        {/* Sidebar controls row — Compact + Clear (D-07: always visible) */}
        <HStack
          px={3} py={2} spacing={2}
          borderTop="1px solid" borderColor="aws.squidLight"
        >
          {/* Compact button */}
          <Button
            leftIcon={<Icon as={MdCompress} />}
            size="sm"
            variant="ghost"
            color="gray.300"
            _hover={{ bg: 'aws.squidLight', color: 'white' }}
            flex={1}
            justifyContent="center"
            fontSize="xs"
            fontWeight="semibold"
            isDisabled={!hasMessages}
            onClick={handleSidebarCompact}
          >
            Compact
          </Button>

          {/* Clear button — with inline confirmation */}
          {clearConfirming ? (
            <VStack flex={1} spacing={1} align="stretch">
              <Text fontSize="xs" color="gray.300" textAlign="center">Clear all messages?</Text>
              <HStack spacing={1} justify="center">
                <Button size="xs" colorScheme="red" onClick={handleSidebarClearConfirm}>
                  Yes, clear
                </Button>
                <Button
                  size="xs"
                  variant="ghost"
                  color="gray.400"
                  onClick={() => setClearConfirming(false)}
                >
                  Cancel
                </Button>
              </HStack>
            </VStack>
          ) : (
            <Button
              leftIcon={<Icon as={MdClear} />}
              size="sm"
              variant="ghost"
              color="red.300"
              _hover={{ bg: 'red.900', color: 'red.200' }}
              flex={1}
              justifyContent="center"
              fontSize="xs"
              fontWeight="semibold"
              isDisabled={!hasMessages}
              onClick={() => setClearConfirming(true)}
            >
              Clear
            </Button>
          )}
        </HStack>

        {/* Footer nav — unchanged */}
        <Box borderTop="1px solid" borderColor="aws.squidLight" p={2}>
          <Button
            leftIcon={<Icon as={MdOutlineArticle} />}
            variant="ghost"
            size="sm"
            w="full"
            justifyContent="flex-start"
            bg={page === 'knowledge' ? 'aws.orange' : 'transparent'}
            color={page === 'knowledge' ? 'aws.squid' : 'gray.300'}
            fontWeight={page === 'knowledge' ? 'bold' : 'normal'}
            _hover={{ bg: page === 'knowledge' ? 'aws.orangeDark' : 'aws.squidLight', color: page === 'knowledge' ? 'aws.squid' : 'white' }}
            onClick={() => setPage('knowledge')}
          >
            Knowledge Base
          </Button>
        </Box>
      </Flex>

      {/* Main content — unchanged structure */}
      <Flex flex={1} direction="column" minW={0} overflow="hidden">
        {/* Top bar — unchanged */}
        <HStack
          as="header"
          h="48px"
          bg="white"
          borderBottom="1px solid"
          borderColor="gray.200"
          px={4}
          justify="space-between"
          flexShrink={0}
          boxShadow="sm"
        >
          <HStack spacing={2}>
            <Text color="gray.400" fontSize="sm">▸</Text>
            <Text fontWeight="medium" color="gray.800" fontSize="sm">
              {page === 'chat' ? 'Advisor' : 'Knowledge Base'}
            </Text>
          </HStack>
          <HStack spacing={2}>
            <Box w={2} h={2} borderRadius="full" bg="green.400" />
            <Text fontSize="xs" color="gray.400">Connected to Neo4j</Text>
          </HStack>
        </HStack>

        <Box as="main" flex={1} overflow="hidden">
          {page === 'chat' ? (
            <ChatBox
              ref={chatRef}
              conversationId={activeConvId}
              onSessionUpdate={handleSessionUpdate}
            />
          ) : (
            <KnowledgeBase />
          )}
        </Box>
      </Flex>
    </Flex>
  )
}

export default App
```
  </action>
  <verify>
    <automated>cd /Users/gleba/Code/sdt520-final-hw/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20 || echo "TypeScript OK"</automated>
  </verify>
  <acceptance_criteria>
    - `MOCK_CONVERSATIONS` and `MockConversation` interface are REMOVED from App.tsx
    - `sessions: Session[]` state exists (starts empty: `useState<Session[]>([])`)
    - `chatRef = useRef<ChatBoxHandle>(null)` exists
    - `<ChatBox ref={chatRef} conversationId={activeConvId} onSessionUpdate={handleSessionUpdate} />` is the ChatBox render call
    - `handleSessionUpdate` updates sessions state (add new or update existing by id)
    - Sidebar shows `sessions.map(...)` rendering real Session objects (not MOCK_CONVERSATIONS)
    - Empty state renders: `"No conversations yet. Send your first message."` (exact copy per UI-SPEC §7)
    - Session subtitle format: `"{N} turn{s} · {relTime}"` with pluralization (per Copywriting §7)
    - Compact button: `leftIcon={<Icon as={MdCompress} />}`, `isDisabled={!hasMessages}`, calls `handleSidebarCompact`
    - Clear button: `leftIcon={<Icon as={MdClear} />}`, `color="red.300"`, `_hover={{bg:"red.900",color:"red.200"}}`, `isDisabled={!hasMessages}`
    - Clear confirmation inline row: "Clear all messages?", "Yes, clear", "Cancel" (per Copywriting §4)
    - `handleNewConversation` calls `crypto.randomUUID()` and `setActiveConvId(newId)`
    - Sidebar controls row wrapper: `HStack px={3} py={2} spacing={2} borderTop="1px solid" borderColor="aws.squidLight"`
    - `import { MdCompress, MdClear } from 'react-icons/md'` present
    - TypeScript compiles: `npx tsc --noEmit` exits 0 or matches baseline (no new errors)
  </acceptance_criteria>
  <done>App.tsx has real sessions state, ChatBox wired with ref + onSessionUpdate, sidebar Compact/Clear controls with inline confirmation, New conversation button, empty state copy; MOCK_CONVERSATIONS removed; TypeScript clean</done>
</task>

</tasks>

<verification>
```bash
cd /Users/gleba/Code/sdt520-final-hw/frontend

# 1. TypeScript compiles
npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 TS errors"

# 2. MOCK_CONVERSATIONS gone
grep -n "MOCK_CONVERSATIONS\|MockConversation" src/App.tsx && echo "FAIL: mock data still present" || echo "OK: mock data removed"

# 3. Real session state present
grep -n "sessions.*Session\[\]\|useRef.*ChatBoxHandle\|onSessionUpdate" src/App.tsx | head -5

# 4. ChatBox ref wired
grep -n "ref={chatRef}\|chatRef\.current\." src/App.tsx | head -5

# 5. Compact/Clear icons imported
grep -n "MdCompress\|MdClear" src/App.tsx | head -3

# 6. Sidebar controls row border
grep -n "borderTop.*aws.squidLight" src/App.tsx | head -3

# 7. Empty state copy
grep -n "No conversations yet" src/App.tsx | head -2

# 8. Session subtitle pluralization
grep -n "turn{s.*turnCount\|turns.*turn" src/App.tsx | head -3
```
</verification>

<success_criteria>
- `MOCK_CONVERSATIONS` and `MockConversation` fully removed
- `sessions: Session[]` state tracks real conversations from ChatBox `onSessionUpdate` callbacks
- `chatRef = useRef<ChatBoxHandle>(null)` wired to `<ChatBox ref={chatRef} ...>`
- Sidebar shows empty state text "No conversations yet. Send your first message." when no sessions
- Sidebar shows session rows with title (first user message) and subtitle "{N} turn(s) · {relTime}"
- Compact sidebar button calls `chatRef.current.compact()`, disabled when no messages
- Clear sidebar button shows inline confirmation then calls `chatRef.current.clear()`, disabled when no messages
- New conversation button calls `crypto.randomUUID()` and resets active session
- TypeScript compiles without new errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-turn-chat/03-04-SUMMARY.md`
</output>
