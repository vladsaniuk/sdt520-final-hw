---
phase: 02.1-ui-polish-visual-design
plan: 03
type: execute
wave: 2
depends_on: [02.1-01]
files_modified:
  - frontend/src/components/Cost/CostTable.tsx
  - frontend/src/components/Chat/ChatBox.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Opening the app shows a welcome screen (not DEMO_MESSAGES) with a bolt icon, title, subtitle, and 4 prompt chips"
    - "Clicking a prompt chip fills the textarea input — does NOT auto-send"
    - "Sending a message makes the welcome screen disappear and chat bubbles appear"
    - "A failed API fetch shows an inline error banner above the input bar — not a chat bubble"
    - "Error banner has a dismiss (×) button that clears it"
    - "CostTable has a dark aws-squid thead with white 'Service' / 'Monthly Cost' headers"
    - "CostTable body rows alternate white (odd) and gray-50 (even)"
    - "CostTable totals row is aws-squid-dark with white bold text and orange total value"
    - "No double-border or double-panel around CostTable — it owns its own container"
    - "Terraform IaC panel header in ChatBox is dark aws-squid matching the other panels"
  artifacts:
    - path: "frontend/src/components/Cost/CostTable.tsx"
      provides: "Self-contained styled table with dark header, striped rows, dark totals row"
      contains: "bg-aws-squid"
    - path: "frontend/src/components/Chat/ChatBox.tsx"
      provides: "Welcome screen, error banner, clean panel composition (no double wrappers)"
      contains: "AWS Architecture Advisor"
  key_links:
    - from: "frontend/src/components/Chat/ChatBox.tsx"
      to: "MermaidViewer"
      via: "direct render (no outer wrapper) — MermaidViewer owns its panel"
      pattern: "<MermaidViewer definition"
    - from: "frontend/src/components/Chat/ChatBox.tsx"
      to: "CostTable"
      via: "direct render (no outer wrapper) — CostTable owns its panel"
      pattern: "<CostTable total"
    - from: "frontend/src/components/Chat/ChatBox.tsx"
      to: "error state"
      via: "catch block sets error, not messages"
      pattern: "setError("
---

<objective>
Restyle CostTable to match the AWS Console table aesthetic and overhaul ChatBox with: welcome screen (empty state), inline error banner, and panel wrapper cleanup (CostTable + MermaidViewer now own their panels so ChatBox's outer wrappers for those sections are removed). The Terraform IaC panel header in ChatBox is also updated to match the dark aws-squid aesthetic.

Purpose: CostTable and MermaidViewer (from Plan 02) are now self-contained. ChatBox must remove its redundant outer wrappers for those sections to prevent double-borders. The welcome screen and error state complete the ChatBox UX per CONTEXT.md decisions.
Output: CostTable.tsx fully restyled. ChatBox.tsx has welcome screen, error banner, clean panel composition.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-ui-polish-visual-design/02.1-UI-SPEC.md
@.planning/phases/02.1-ui-polish-visual-design/02.1-CONTEXT.md

<interfaces>
<!-- Tailwind color tokens (frontend/tailwind.config.js — already defined): -->
<!-- bg-aws-squid = #232F3E -->
<!-- bg-aws-squid-light = #37475A -->
<!-- bg-aws-squid-dark = #1A232E -->
<!-- text-aws-orange = #FF9900 -->
<!-- bg-aws-orange = #FF9900 -->
<!-- bg-aws-orange-dark = #E8891A -->
<!-- bg-aws-orange/10 = 10% opacity orange (Tailwind opacity modifier) -->

<!-- CostTable current props (unchanged): -->
<!-- interface CostTableProps { total: number; breakdown: BreakdownItem[] } -->
<!-- interface BreakdownItem { service: string; cost: number; is_calculated: boolean } -->

<!-- ChatBox: these state variables already exist — add only error state: -->
<!-- const [input, setInput] = useState('') -->
<!-- const [messages, setMessages] = useState<Message[]>([])  ← change from DEMO_MESSAGES to [] -->
<!-- const [loading, setLoading] = useState(false) -->
<!-- const [toast, setToast] = useState<string | null>(null) -->
<!-- ADD: const [error, setError] = useState<string | null>(null) -->

<!-- MermaidViewer (Plan 02 output) — now a self-contained panel with its own wrapper and header -->
<!-- CostTable (this plan output) — becomes a self-contained panel with its own wrapper and header -->
<!-- Both: render directly, no outer wrapper in ChatBox -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle CostTable — dark thead, striped rows, totals row, self-contained wrapper</name>
  <files>frontend/src/components/Cost/CostTable.tsx</files>

  <read_first>
    - frontend/src/components/Cost/CostTable.tsx — read current component in full; note existing wrapper div classes to replace
    - .planning/phases/02.1-ui-polish-visual-design/02.1-UI-SPEC.md — section "CostTable" for exact classes, stripe logic, totals row treatment
  </read_first>

  <action>
Replace the entire component return. Props interface (`CostTableProps`, `BreakdownItem`) and imports are unchanged.

**New complete component (replace function body only):**
```tsx
export const CostTable: React.FC<CostTableProps> = ({ total, breakdown }) => {
  return (
    <div className="rounded-xl overflow-hidden border border-gray-200 shadow-sm my-2">
      <table className="min-w-full">
        <thead className="bg-aws-squid">
          <tr>
            <th className="px-4 py-3 text-left text-xs font-bold text-white uppercase tracking-wider">
              Service
            </th>
            <th className="px-4 py-3 text-right text-xs font-bold text-white uppercase tracking-wider">
              Monthly Cost
            </th>
          </tr>
        </thead>
        <tbody>
          {breakdown.map((item, i) => (
            <tr key={i} className={i % 2 === 0 ? 'bg-white' : 'bg-gray-50'}>
              <td className="px-4 py-3 text-sm text-gray-800">
                {item.service}
                {!item.is_calculated && (
                  <span className="ml-2 text-xs text-gray-400 bg-gray-100 px-2 py-1 rounded font-normal">
                    (est.)
                  </span>
                )}
              </td>
              <td className="px-4 py-3 text-sm text-gray-800 text-right">
                ${item.cost.toFixed(2)}
              </td>
            </tr>
          ))}
        </tbody>
        <tfoot>
          <tr className="bg-aws-squid-dark">
            <td className="px-4 py-3 text-sm font-bold text-white">
              Total Estimate
            </td>
            <td className="px-4 py-3 text-sm font-bold text-aws-orange text-right">
              ${total.toFixed(2)}
            </td>
          </tr>
        </tfoot>
      </table>
    </div>
  )
}
```

**Key changes from current:**
- Outer wrapper: `bg-white rounded-lg border shadow-sm overflow-hidden my-4` → `rounded-xl overflow-hidden border border-gray-200 shadow-sm my-2` (removes redundant bg-white, updates radius, reduces margin)
- `<thead>`: `bg-gray-50` → `bg-aws-squid`
- `<th>` text: `text-gray-500 font-medium` → `text-white font-bold`
- `<th>` "Monthly Cost": add `text-right` (right-aligned)
- `<tbody>` remove `divide-y divide-gray-200` — stripes replace dividers
- `<tr>`: add `i % 2 === 0 ? 'bg-white' : 'bg-gray-50'` stripe logic
- `<td>` cells: `px-6 py-4` → `px-4 py-3` (tighter spacing); `text-gray-900` → `text-gray-800`
- `is_calculated: false` badge: orange italic `(Not Calculated)` → gray pill `(est.)`
- Totals row: moved to `<tfoot>`, `bg-gray-50 font-bold` → `bg-aws-squid-dark`; cells `text-gray-900` → `text-white font-bold`; total value `text-aws-orange`
  </action>

  <verify>
    <automated>
      # Dark header
      grep -n "bg-aws-squid" frontend/src/components/Cost/CostTable.tsx

      # Stripe logic
      grep -n "i % 2 === 0" frontend/src/components/Cost/CostTable.tsx

      # Totals row dark background
      grep -n "bg-aws-squid-dark" frontend/src/components/Cost/CostTable.tsx

      # Orange total value
      grep -n "text-aws-orange" frontend/src/components/Cost/CostTable.tsx

      # New est. badge (not old orange italic)
      grep -n "(est.)" frontend/src/components/Cost/CostTable.tsx
      # Old badge should be gone
      grep -n "Not Calculated\|text-orange-500" frontend/src/components/Cost/CostTable.tsx

      # Self-contained wrapper (rounded-xl not rounded-lg)
      grep -n "rounded-xl" frontend/src/components/Cost/CostTable.tsx

      # TypeScript check
      cd frontend && npx tsc --noEmit
    </automated>
  </verify>

  <acceptance_criteria>
    - File contains `rounded-xl overflow-hidden border border-gray-200 shadow-sm my-2` on outer wrapper div
    - File contains `bg-aws-squid` on `<thead>`
    - File does NOT contain `bg-gray-50` on `<thead>` (old style)
    - File contains `text-white font-bold` on `<th>` elements
    - File contains `text-right` on the "Monthly Cost" `<th>`
    - File contains `i % 2 === 0 ? 'bg-white' : 'bg-gray-50'` stripe logic on `<tr>`
    - File contains `bg-aws-squid-dark` on totals row
    - File contains `text-aws-orange` on total value `<td>`
    - File contains `(est.)` as the badge text
    - File does NOT contain `text-orange-500` or `Not Calculated` (old badge)
    - File does NOT contain `bg-white rounded-lg` (old outer wrapper)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>CostTable is a self-contained panel with dark aws-squid thead, alternating striped rows, and a dark aws-squid-dark totals row with orange highlighted total. The (est.) badge replaces the old orange italic label.</done>
</task>

<task type="auto">
  <name>Task 2: ChatBox overhaul — welcome screen, error banner, panel cleanup, IaC header</name>
  <files>frontend/src/components/Chat/ChatBox.tsx</files>

  <read_first>
    - frontend/src/components/Chat/ChatBox.tsx — read the ENTIRE file before editing; all 4 changes must be made together consistently
    - .planning/phases/02.1-ui-polish-visual-design/02.1-UI-SPEC.md — sections "Welcome / Empty Screen", "Inline Error Banner", and the Interaction States table
    - .planning/phases/02.1-ui-polish-visual-design/02.1-CONTEXT.md — "Empty & Welcome State" and "Error States" decisions
  </read_first>

  <action>
Make 4 distinct changes to ChatBox.tsx. Read the full current file first, then apply all changes.

---

**CHANGE 1 — Initial messages state (line ~123):**

Change:
```tsx
const [messages, setMessages] = useState<Message[]>(DEMO_MESSAGES)
```
to:
```tsx
const [messages, setMessages] = useState<Message[]>([])
```

`DEMO_MESSAGES` constant can remain defined in the file (do NOT delete it) — just stop using it as initial state.

---

**CHANGE 2 — Add error state (add after the `toast` state line):**

```tsx
const [error, setError] = useState<string | null>(null)
```

---

**CHANGE 3 — Update handleSend (clear error on send; set error in catch instead of pushing message):**

At the START of `handleSend`, before `const userMessage`:
```tsx
setError(null)
```

In the `catch` block, replace:
```tsx
setMessages((prev) => [
  ...prev,
  { role: 'assistant', content: '⚠️ Could not reach the backend. Make sure the API is running.' },
])
```
with:
```tsx
setError('Something went wrong — please try again.')
```

---

**CHANGE 4 — Replace the messages area JSX:**

Current structure (inside the main `<div className="flex flex-col h-full bg-gray-50">`):
```tsx
{/* Messages */}
<div className="flex-1 overflow-y-auto px-4 py-6 space-y-6">
  {messages.map(...)}
  {loading && (...)}
  <div ref={bottomRef} />
</div>
```

Replace with:
```tsx
{/* Messages / Welcome */}
<div className={`flex-1 overflow-y-auto ${messages.length > 0 ? 'px-4 py-6 space-y-6' : ''}`}>
  {messages.length === 0 ? (
    /* Welcome screen */
    <div className="flex flex-col items-center justify-center h-full gap-6 px-4">
      {/* Logo area */}
      <div className="flex flex-col items-center gap-3">
        <div className="w-14 h-14 rounded-2xl bg-aws-orange flex items-center justify-center">
          <svg className="w-8 h-8 text-aws-squid" viewBox="0 0 24 24" fill="currentColor">
            <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" />
          </svg>
        </div>
        <h1 className="text-2xl font-bold text-aws-squid">AWS Architecture Advisor</h1>
        <p className="text-sm text-gray-500 text-center max-w-xs leading-relaxed">
          Describe your AWS workload to get a production-ready architecture plan.
        </p>
      </div>
      {/* Prompt chips */}
      <div className="flex flex-wrap gap-2 justify-center max-w-lg">
        {[
          'Serverless e-commerce API',
          'Multi-region DR setup',
          'ML inference pipeline',
          'Event-driven microservices',
        ].map((chip) => (
          <button
            key={chip}
            onClick={() => setInput(chip)}
            className="px-4 py-2 rounded-full border border-gray-300 text-sm text-gray-700 hover:border-aws-orange hover:text-aws-squid hover:bg-aws-orange/10 transition-colors cursor-pointer"
          >
            {chip}
          </button>
        ))}
      </div>
    </div>
  ) : (
    /* Message list */
    <>
      {messages.map((msg, i) => (
        /* KEEP all existing message bubble JSX exactly as-is */
        /* Only change: diagram and cost sections — remove outer wrapper + panel header */
        <div key={i} className={`flex gap-3 ${msg.role === 'user' ? 'justify-end' : 'justify-start'}`}>
          {/* Avatar — assistant (unchanged) */}
          {msg.role === 'assistant' && (
            <div className="w-8 h-8 rounded-full bg-aws-orange flex items-center justify-center shrink-0 mt-1 shadow-sm">
              <svg className="w-4 h-4 text-aws-squid" viewBox="0 0 24 24" fill="currentColor">
                <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" />
              </svg>
            </div>
          )}

          <div className={`max-w-3xl ${msg.role === 'user' ? 'order-first' : ''}`}>
            {/* Bubble (unchanged) */}
            <div
              className={`rounded-2xl px-4 py-3 text-sm leading-relaxed ${
                msg.role === 'user'
                  ? 'bg-aws-squid text-white rounded-tr-sm ml-auto'
                  : 'bg-white border border-gray-200 text-gray-800 rounded-tl-sm shadow-sm'
              }`}
            >
              <p className="whitespace-pre-wrap">{msg.content}</p>
            </div>

            {/* Rich panels — only on assistant messages */}
            {msg.role === 'assistant' && (
              <>
                {/* DIAGRAM — MermaidViewer owns its panel; no outer wrapper */}
                {msg.diagram && (
                  <MermaidViewer definition={msg.diagram} />
                )}

                {/* COST — CostTable owns its panel; no outer wrapper */}
                {msg.costs && msg.costs.breakdown.length > 0 && (
                  <CostTable total={msg.costs.total} breakdown={msg.costs.breakdown} />
                )}

                {/* IaC / TERRAFORM — ChatBox keeps this panel (has download button) */}
                {msg.iac && msg.iac.length > 0 && (
                  <div className="mt-3 rounded-xl overflow-hidden border border-gray-200 shadow-sm">
                    {/* Updated header: aws-squid to match other panels */}
                    <div className="flex items-center justify-between px-4 py-2 bg-aws-squid border-b border-aws-squid-light">
                      <div className="flex items-center gap-2">
                        <svg className="w-4 h-4 text-aws-orange" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                        </svg>
                        <span className="text-xs font-bold text-gray-300 uppercase tracking-widest">TERRAFORM</span>
                      </div>
                      {/* Download button — Phase 5 will wire real generation */}
                      <button
                        onClick={() => showToast('⬇️ Terraform download coming in Phase 5')}
                        className="flex items-center gap-1.5 text-xs px-3 py-1 rounded-md bg-aws-orange text-aws-squid font-semibold hover:bg-aws-orange-dark transition-colors"
                      >
                        <svg className="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
                        </svg>
                        Download .tf
                      </button>
                    </div>
                    <div className="p-4 space-y-3">
                      {msg.iac.map((snippet, j) => (
                        <CodeSnippet
                          key={j}
                          code={snippet.content}
                          language={snippet.type === 'terraform' ? 'hcl' : 'yaml'}
                        />
                      ))}
                    </div>
                  </div>
                )}
              </>
            )}
          </div>

          {/* Avatar — user (unchanged) */}
          {msg.role === 'user' && (
            <div className="w-8 h-8 rounded-full bg-gray-300 flex items-center justify-center shrink-0 mt-1 text-gray-600 font-bold text-sm">
              U
            </div>
          )}
        </div>
      ))}

      {/* Loading indicator (unchanged) */}
      {loading && (
        <div className="flex gap-3 justify-start">
          <div className="w-8 h-8 rounded-full bg-aws-orange flex items-center justify-center shrink-0 shadow-sm">
            <svg className="w-4 h-4 text-aws-squid" viewBox="0 0 24 24" fill="currentColor">
              <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" />
            </svg>
          </div>
          <div className="bg-white border border-gray-200 rounded-2xl rounded-tl-sm px-4 py-3 shadow-sm">
            <div className="flex gap-1 items-center h-5">
              <span className="w-2 h-2 bg-gray-400 rounded-full animate-bounce [animation-delay:0ms]" />
              <span className="w-2 h-2 bg-gray-400 rounded-full animate-bounce [animation-delay:150ms]" />
              <span className="w-2 h-2 bg-gray-400 rounded-full animate-bounce [animation-delay:300ms]" />
            </div>
          </div>
        </div>
      )}
      <div ref={bottomRef} />
    </>
  )}
</div>
```

---

**CHANGE 5 — Add error banner between messages div and input bar:**

Between the closing `</div>` of the messages area and the opening `<div className="shrink-0 bg-white border-t...">` of the input bar, insert:

```tsx
{/* Inline error banner */}
{error && (
  <div className="mx-4 mb-3 flex items-center gap-2 px-4 py-3 rounded-lg bg-red-50 border border-red-200 text-red-700 text-sm">
    <svg className="w-4 h-4 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5C2.962 18.333 3.924 20 5.464 20z" />
    </svg>
    <span className="flex-1">Something went wrong — please try again.</span>
    <button
      onClick={() => setError(null)}
      className="ml-auto text-red-400 hover:text-red-600"
    >
      ×
    </button>
  </div>
)}
```

**What NOT to change:**
- The `DEMO_MESSAGES` constant — keep it defined, just remove from useState initial value
- The `showToast` function and toast notification JSX at the bottom
- The input bar JSX (textarea, send button, hint text)
- The `Message`, `IaC`, `CostBreakdown`, `Costs` interface definitions
- All existing imports
  </action>

  <verify>
    <automated>
      # Empty initial state (no DEMO_MESSAGES in useState)
      grep -n "useState<Message\[\]>" frontend/src/components/Chat/ChatBox.tsx

      # Error state present
      grep -n "const \[error, setError\]" frontend/src/components/Chat/ChatBox.tsx

      # Error cleared on send
      grep -n "setError(null)" frontend/src/components/Chat/ChatBox.tsx

      # Catch sets error not messages
      grep -n "setError('Something went wrong" frontend/src/components/Chat/ChatBox.tsx

      # Welcome screen text
      grep -n "AWS Architecture Advisor\|Describe your AWS workload" frontend/src/components/Chat/ChatBox.tsx

      # All 4 prompt chips
      grep -n "Serverless e-commerce API\|Multi-region DR setup\|ML inference pipeline\|Event-driven microservices" frontend/src/components/Chat/ChatBox.tsx

      # Chip onClick sets input not submit
      grep -n "setInput(chip)" frontend/src/components/Chat/ChatBox.tsx

      # Error banner classes
      grep -n "bg-red-50 border border-red-200" frontend/src/components/Chat/ChatBox.tsx

      # MermaidViewer rendered directly (no outer wrapper div before it)
      grep -n "<MermaidViewer definition" frontend/src/components/Chat/ChatBox.tsx

      # CostTable rendered directly (no outer wrapper div before it)
      grep -n "<CostTable total" frontend/src/components/Chat/ChatBox.tsx

      # Terraform panel updated to aws-squid
      grep -n "TERRAFORM" frontend/src/components/Chat/ChatBox.tsx

      # TypeScript check
      cd frontend && npx tsc --noEmit
    </automated>
  </verify>

  <acceptance_criteria>
    - `useState<Message[]>([])` — initial state is empty array, NOT DEMO_MESSAGES
    - `const [error, setError] = useState<string | null>(null)` present
    - `setError(null)` called at the start of handleSend (before userMessage is created)
    - `setError('Something went wrong — please try again.')` in the catch block
    - catch block does NOT call `setMessages` to push an error assistant message
    - File contains `AWS Architecture Advisor` (welcome screen title)
    - File contains `Describe your AWS workload to get a production-ready architecture plan.` (subtitle exact copy)
    - File contains all 4 prompt chip strings: "Serverless e-commerce API", "Multi-region DR setup", "ML inference pipeline", "Event-driven microservices"
    - Chip onClick calls `setInput(chip)` — no auto-submit
    - Error banner contains `bg-red-50 border border-red-200 text-red-700`
    - Error banner has dismiss button calling `setError(null)`
    - `<MermaidViewer definition={msg.diagram} />` rendered without a parent outer-wrapper div (MermaidViewer owns its panel)
    - `<CostTable total={msg.costs.total}` rendered without a parent outer-wrapper div (CostTable owns its panel)
    - Terraform panel header contains `bg-aws-squid` and `TERRAFORM` (uppercase)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>ChatBox starts with welcome screen (bolt icon, title, subtitle, 4 prompt chips). Messages list renders only after first send. Fetch failures show inline error banner (not a chat bubble) with dismiss button. MermaidViewer and CostTable are rendered directly without redundant outer wrappers. Terraform panel header matches dark aws-squid aesthetic.</done>
</task>

</tasks>

<verification>
```bash
# Welcome screen and error state
grep -c "AWS Architecture Advisor\|setError\|bg-red-50\|TERRAFORM" frontend/src/components/Chat/ChatBox.tsx

# CostTable styling
grep -c "bg-aws-squid\|bg-aws-squid-dark\|text-aws-orange\|i % 2 === 0" frontend/src/components/Cost/CostTable.tsx

# TypeScript clean build
cd frontend && npx tsc --noEmit && echo "TS OK"

# No double-border: ChatBox should NOT have the old cost/diagram outer panel wrappers
grep -n "Estimated Monthly Cost\|Architecture Diagram" frontend/src/components/Chat/ChatBox.tsx
# Expected: 0 results (those labels moved to sub-components or removed)
```
</verification>

<success_criteria>
- App opens to welcome screen: bolt icon in orange circle, "AWS Architecture Advisor" heading, subtitle, 4 prompt chips
- Clicking chip fills textarea (does not send)
- First message sent → welcome screen replaced by message list
- API error → red banner above input bar with dismiss ×; no error bubbles in message list
- CostTable: dark aws-squid header row, alternating white/gray-50 body rows, dark aws-squid-dark totals row, orange total value
- No double-border on diagram or cost panels (each component owns its container)
- Terraform panel header is dark aws-squid with "TERRAFORM" label
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-ui-polish-visual-design/02.1-03-SUMMARY.md`
</output>
