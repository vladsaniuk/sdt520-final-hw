---
date: "2026-05-10 22:54"
promoted: false
---

Notable findings from the agents: sync Neo4j call blocking async event loop, per-request ChatOpenAI instantiation, unbounded _terraform_cache, CORS wildcard default, SQLite new-connection-per-call, N+1 queries in list_conversations, require('http') CommonJS in ESM Vite config, double embedder instances (~90MB × 2). Zero frontend tests. Neo4j has no seeded AWS_Service or WellArchitected_Pillar nodes (Phase 2 graph seeding not done).
